[project]
name = "morphkv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets==3.6.0",
    "torch>=2.9.1",
    "transformers==4.45",
    "flash-attn>=2.0.0",
    "psutil",
    "packaging",
    "ninja",
]

[tool.uv.extra-build-dependencies]
flash-attn = ["torch"]
